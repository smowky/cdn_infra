# HTTP redirect
server {
    listen      80;
    listen      [::]:80;
    server_name {{ nginx_default_server_name }};

    location / {
        return 301 https://{{ nginx_default_server_name }}$request_uri;
    }
}
